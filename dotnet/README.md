sentry-cli for .NET
===================

## dotnet tool

Get `sentry-cli` through:

```
dotnet tool install -g sentry.cli
sentry-cli --help`
```

## MSBuild tasks

* Download `sentry-cli`
* Invoke `sentry-cli` commands through MSBuild.